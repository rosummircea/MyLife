import {Tag,Utensils,House,Car,HeartPulse,Shield,Gift,Plane,ShoppingBag,Dumbbell,PawPrint,Baby,Wallet,Zap,Coffee,GraduationCap} from 'lucide-react'
import {categoryAppearance,categoryRoot} from '@/lib/category-display'
import type {CategoryRow} from '@/lib/mylife-data'
const icons={tag:Tag,food:Utensils,home:House,car:Car,health:HeartPulse,shield:Shield,gift:Gift,plane:Plane,shopping:ShoppingBag,sport:Dumbbell,pet:PawPrint,child:Baby,wallet:Wallet,utilities:Zap,coffee:Coffee,education:GraduationCap}
export default function CategoryIcon({category,categories=[],subcategory=false}:{category?:Pick<CategoryRow,'id'|'name'|'icon'|'color'> & Partial<Pick<CategoryRow,'parent_id'>>;categories?:CategoryRow[];subcategory?:boolean}){const root=category?.parent_id?categoryRoot(category.id,categories):undefined;const {icon,color}=categoryAppearance(root??category);const Icon=icons[icon as keyof typeof icons]??Tag;const child=subcategory||!!category?.parent_id;return <span className={`categoryIcon ${child?'categoryColorOnly':''}`} style={{color:'#101b2c',backgroundColor:color,display:'inline-flex',flexShrink:0,alignItems:'center',justifyContent:'center',width:30,height:30,borderRadius:9}} aria-hidden="true">{!child&&<Icon size={17}/>}</span>}
